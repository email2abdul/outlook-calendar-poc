node_modules/
.env
*.log
.DS_Store
*.db
*.db-journal
.vercel

# Lumendi product brochure PDFs — source material for `npm run ingest:products`,
# not committed (large binaries; the extracted config/product-context.json is).
brochures/
data/demo/
# Recorded registry answers (offline replay) — record locally, never commit
data/cassettes/
